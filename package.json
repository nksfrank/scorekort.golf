{
  "name": "scorekort.golf",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "engines": {
    "node": "18.14.2",
    "yarn": "1.22.19"
  },
  "scripts": {
    "dev:backend": "yarn --cwd backend dev",
    "dev:frontend": "yarn --cwd frontend dev",
    "start": "npm-run-all -l -p dev:*"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
